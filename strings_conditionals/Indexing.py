# Indexing in strings

str = "Python"
ch = str[0] #ch is assigned the first character of the string

#str[4] = "a" #we cannot change the value of a character in a string because strings are immutable in Python
#print(str) # This will cause a TypeError because strings in Python are immutable
print(ch) # This will print 'P', the first character of the string